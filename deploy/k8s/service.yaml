apiVersion: v1
kind: Service
metadata:
  name: validador-final
  namespace: validador-final
spec:
  type: ClusterIP
  selector:
    app: validador-final
  ports:
    - name: http
      port: 8000
      targetPort: http
