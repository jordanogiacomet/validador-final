# Local development default. Published containers should prefer
# VALIDATOR_FRONTEND_API_BASE_URL so the same frontend image can move between
# environments without being rebuilt.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
