Started: 2026-04-13 00:00 BRT
Last Updated: 2026-04-18 13:05 -03

---

## Codebase Patterns

- Preserve canonical inventory field semantics across all tenants: Item, Placa Anterior, Descrição, Marca, Modelo, NS, Local, CC, Complemento, Observação.
- Treat tenant-specific behavior as configuration first, not as branching logic in the validation core.
- Keep one generic validation engine for all tenants.
- Normalize source input into canonical field names before running validation rules.
- Derive `flag_item_coletado` and `flag_item_cadastrado_do_zero` before executing row-level validation.
- Prefer rule-based validation with reusable abstractions over hardcoded validation chains.
- Keep API handlers thin and delegate orchestration to services.
- Keep validation logic, job orchestration, and report rendering in separate modules.
- Preserve the distinction between collected items and zero-created items as a core domain concept.
- Use `progress.txt` to record assumptions, gotchas, and reusable learnings for future iterations.
- Only mark a story as passed when implementation is complete and quality checks pass.
- Quality gates are: `ruff check app tests` (lint) and `pytest` (tests). No mypy/pyright installed.
- Run `ruff check --fix app tests` to auto-fix import sorting issues before committing.
- Use `Path(__file__).resolve().parent` for module-relative paths instead of hardcoded relative paths.
- Tenant configs use typed Pydantic sub-models (`CategoryConfig`, `LLMConfig`) — keep them in `app/core/tenant_config.py`.
- Clear `RULE_REGISTRY` in test setup/teardown to avoid cross-test contamination.
- Use `register_rule()` from `app.core.registry` to add rules; the engine resolves them by name from `TenantConfig.enabled_rules`.
- Pydantic v2 copies dict values on model construction — pass shared_context between ValidationContext instances explicitly for cross-row caching.
- Cross-row rules (e.g., duplicate detection) should cache computed data in `shared_context` to avoid re-computing per row.
- Use Protocol-based abstractions for external service clients (e.g., LLM) to enable testability without mocking internals.
- Tenant-specific prompt files live under `app/tenants/{tenant_id}/prompts/`; reference via `prompt_file` in tenant YAML.
- Parse optional YAML frontmatter for tenant prompt files at load time and default legacy prompts to a stable version token so LLM audit/report metadata stays reproducible without breaking older prompts.
- LLM response caching is tenant-configured with `llm.cache_ttl_seconds`; cache keys and persistence live in `app/core/llm_cache.py`, storage path comes from `VALIDATOR_LLM_CACHE_PATH`, and `force_refresh` job params bypass cache reads while refreshing successful writes.
- LLM fallback model selection is tenant-configured with `llm.fallback_model`; retry only transient failures (timeouts, HTTP 429, HTTP 5xx), and preserve attempted model sequence plus primary/final failure details in warning issue metadata.
- Frontend media parsing should stay frontend-only over raw row payloads: preserve all browser-renderable sources in order (HTTP/HTTPS, `data:image`, raw base64) and ignore local mobile-app paths like `/data/user/...`.
- Duplicate-result severity UI should reuse a shared conflict helper so red cards and duplicate filters stay aligned on the same rule.
- Frontend result filtering/search should stay in shared presentation helpers over the existing result payload, and local filters/search should reset when `currentJobId` changes.
- Frontend category result filters derive available categories from `CATEGORY_*` grouped problem codes unless the backend explicitly adds category metadata to the result payload.
- Mutable review markers for completed results should be stored as tenant-scoped sidecar files under `results/{tenant_id}/{job_id}/review_flags.json` and overlaid into the result payload at read time, leaving generated JSON/PDF artifacts immutable.
- Frontend review-marker filtering should stay in `frontend/src/lib/presentation.ts` over `review_flags`, and local review filters should reset together with search/result filters when `currentJobId` changes.
- For `Nomes iguais`, duplicate resolution should be framed as consolidation: keep one physical CSV row only as an implementation detail, ignore photo/media and date/time enrichment, and preserve the survivor's non-empty allowed value on conflicts.
- Bulk consolidation over duplicate groups should stay conservative for now: only process `Nomes iguais` groups with exactly 2 occurrences, and execute them from the highest row indices downward so CSV reindexing does not invalidate the remaining groups.
- Structured logging flows through `app/core/logging.py`: use `get_logger("layer_name")` + `log_event(logger, "<layer>.<event>", tenant_id=..., job_id=..., duration_ms=..., error=...)` so every emission shares the canonical fields and optionally picks up the current request_id from the contextvar.
- Cross-request correlation comes from `app/api/middleware.py` (request_id contextvar + `X-Request-ID` header echoed back on responses); do not introduce a parallel request-id mechanism.
- Prometheus instrumentation should stay centralized in `app/core/metrics.py`, remain gated by `VALIDATOR_METRICS_ENABLED`, and never use `job_id`/`request_id` as metric labels.
- Keep `/health` thin by delegating probes to `app/core/health.py`; uploads/results/job_store are critical checks, while LLM reachability is an optional degradation path that is opt-in per tenant via `llm.healthcheck_enabled`.
- LLM health probes should stay conservative: use low-cost provider reachability checks with aggressive timeout defaults instead of triggering a full audit completion.
- Tenant-scoped API auth should derive tenant context from `X-API-Key`; any `tenant_id` request parameter is only a hint and must be rejected on mismatch.
- Persist auth-related operational metadata with the configured `api_key_id`, never the raw API key secret.
- Middleware that enforces custom auth headers must allow unauthenticated `OPTIONS` preflight requests and keep `/health` and `/metrics` public.
- Login-issued API keys flow through `app/services/auth_service.py`: tenant operators live in `TenantConfig.operators` with PBKDF2 password hashes, `/login` is public, issued keys are stored by hash only via optional `VALIDATOR_API_KEY_STORE_PATH`, and middleware resolves issued keys before legacy tenant `api_keys`.
- Issued key lifecycle policy belongs in `tenant.auth.issued_api_key_ttl_seconds`; `AuthService` owns issue/expire/revoke transitions plus audit emission, and middleware should reject expired or revoked issued keys before falling back to legacy tenant `api_keys`.
- Derive upload/result/report artifact paths through shared tenant-scoped helpers (`uploads/{tenant_id}/...`, `results/{tenant_id}/...`) and keep a legacy root-path fallback during storage-layout migrations.
- Tenant text alias dictionaries belong in `tenant.normalization`; normalize `Marca`/`Modelo` in the core pipeline before rules and preserve the original source values in `ValidationContext.raw_row`.
- When a rule needs to infer the expected `Marca` from `Modelo`, keep the tenant-owned dictionary in `tenant.normalization.model_brands` keyed by canonical model names and compare against the already-normalized canonical row value.
- New tenant-owned rule config should use typed Pydantic sub-models in `app/core/tenant_config.py`; validate regex-like entries during tenant load so jobs do not fail mid-validation.
- Frontend auth should stay centralized in `frontend/src/lib/api.ts`: treat `/login` as the only public call, inject the issued `X-API-Key` for the workspace from shared helpers, and cover UI auth states with Vitest `jsdom` component tests plus `frontend/src/test/setup.ts`.
- Protected frontend downloads must use authenticated helpers in `frontend/src/lib/api.ts`; raw `<a href>` links do not send the issued `X-API-Key` header.
- Session renewal routes through `AuthService.renew_api_key` + `POST /api-keys/renew`; emit `api_key_renewed` linking predecessor to successor, propagate `expires_at` into the frontend `LoginResponse`, and warn the operator before expiration without forcing a logout.

---

## Project Context

This project is a multi-tenant inventory validator.

Primary goals:
- support tenant-specific validation without duplicating the core engine
- preserve canonical inventory domain semantics
- support deterministic validation and optional LLM audit
- support asynchronous validation jobs
- generate operationally useful outputs for correction workflows

Canonical fields:
- Item
- Placa Anterior
- Descrição
- Marca
- Modelo
- NS
- Local
- CC
- Complemento
- Observação

Derived concepts:
- flag_item_coletado
- flag_item_cadastrado_do_zero

Expected architectural direction:
- app/core/
- app/rules/
- app/services/
- app/api/
- app/workers/
- app/tenants/
- tests/

---

## Initial Notes

- This repository is intended to be developed iteratively through Ralph loop style execution.
- The PRD in `prd.json` is the source of truth for story order and acceptance criteria.
- The default tenant must remain a working baseline for future tenant-specific extensions.
- Tenant variation should prefer config for mappings, thresholds, categories, critical checks, enabled rules, and LLM settings.
- Do not scatter tenant names through the validation core.
- Avoid refactoring unrelated areas during early foundation stories.

---

## 2026-04-13 - US-001
- Implemented canonical inventory domain model in `app/core/canonical_fields.py`
- Added `CanonicalInventoryRow` Pydantic model with all 10 canonical fields and 2 derived flags
- Added `derive_flags(placa_anterior)` to compute `flag_item_coletado` and `flag_item_cadastrado_do_zero`
- Added `CANONICAL_FIELD_MAP` for display-name-to-attribute mapping
- Added `normalize_row()` to convert raw input rows to canonical form with derived flags
- Documented collected item vs zero-created item semantics in model docstring
- Added `pyyaml` dependency to `pyproject.toml` (was missing for tenant_loader)
- Fixed ruff import sorting in 3 files
- Files changed: `app/core/canonical_fields.py`, `pyproject.toml`, `.gitignore`
- **Learnings for future iterations:**
  - The project had no type checker (mypy/pyright) installed; ruff lint + pytest are the quality gates
  - `pyyaml` was missing from dependencies despite `tenant_loader.py` importing yaml
  - Ruff auto-fix (`--fix`) handles import sorting cleanly
  - The `.gitignore` was empty at project start; populated it with Python defaults
---

## 2026-04-13 - US-002
- Enhanced `TenantConfig` with typed `CategoryConfig` and `LLMConfig` sub-models instead of raw dicts
- Added `DEFAULT_TENANT_ID` constant and `load_default_tenant_config()` helper
- Added `list_tenants()` to discover available tenant configurations
- Changed `TENANTS_DIR` to use `Path(__file__).resolve()` for reliable path resolution
- Created `empresa_exemplo` seed tenant with categories (ar_condicionado, tv), critical checks, and LLM enabled
- Added comprehensive tests in `tests/test_tenant.py` covering config construction, loading, default fallback, error case, and listing
- Files changed: `app/core/tenant_config.py`, `app/core/tenant_loader.py`, `app/tenants/default/tenant.yaml`, `app/tenants/empresa_exemplo/tenant.yaml`, `tests/test_tenant.py`
- **Learnings for future iterations:**
  - Use `Path(__file__).resolve().parent` for tenant directory paths to avoid CWD-dependent path issues
  - Categories and LLM settings should use typed Pydantic sub-models, not raw dicts, for validation safety
  - The `tests/` directory is at root level (not `app/tests/`) per `pyproject.toml` `testpaths` config, but `app/tests/` also exists with the health test
---

## 2026-04-13 - US-003
- Enhanced `ValidationContext` with `all_rows` field for cross-row rule access (e.g., duplicate detection)
- Enhanced `ValidationEngine` with typed `BaseRule` references and `validate_all()` for batch processing
- `validate_all()` normalizes raw rows via `normalize_row()` and passes all rows + shared context to each rule
- Enhanced `registry.py` with `register_rule()` and `get_rule()` helpers, typed `BaseRule` values
- Added 10 tests in `tests/test_engine.py` covering: rule registration, enabled/disabled rules, unknown rules, row validation with/without applicable rules, batch validation, all_rows context passing, context field verification
- Files changed: `app/core/context.py`, `app/core/engine.py`, `app/core/registry.py`, `tests/test_engine.py`
- **Learnings for future iterations:**
  - Use `setup_function`/`teardown_function` to clear `RULE_REGISTRY` between tests to avoid cross-test contamination
  - `BaseRule` uses class attributes for `name` (not instance), so test rule subclasses declare `name: str = "rule_name"`
  - The engine skeletons from US-001/US-002 were mostly correct but needed typed rule references and batch support
---

## 2026-04-13 - US-004
- Implemented `DuplicateItemRule` in `app/rules/integrity.py` — detects duplicate Item values across all rows using Counter cached in shared_context
- Implemented `FlagConsistencyRule` in `app/rules/integrity.py` — validates that flag_item_coletado and flag_item_cadastrado_do_zero are consistent with Placa Anterior presence
- Issues include code, severity, message, and field as required by acceptance criteria
- Added 16 tests in `tests/test_integrity_rules.py` covering: applies logic, unique/duplicate items, cache behavior, None items, consistent/inconsistent flags, empty-string placa handling, issue field population, and engine integration
- Files changed: `app/rules/integrity.py`, `tests/test_integrity_rules.py`
- **Learnings for future iterations:**
  - Pydantic v2 copies dict values when constructing models — shared_context passed to ValidationContext is a new dict, not the original reference. Use the context's own shared_context for caching, and pass it between contexts explicitly.
  - The DuplicateItemRule caches counts in shared_context to avoid re-counting for every row — this pattern is important for cross-row rules.
  - FlagConsistencyRule checks both directions: placa present with wrong flags AND placa absent with wrong flags, plus conflict states where both flags are set.
---

## 2026-04-13 - US-005
- Implemented `ZeroItemQualityRule` in `app/rules/zero_item_quality.py`
- Checks: empty Complemento, short Complemento (configurable threshold), missing Marca, missing Modelo
- Relative complement quality comparison: compares word count against peer average for same item type (Descrição), warns when below 50% of average
- Caches peer complement stats in shared_context for cross-row efficiency
- Updated default tenant to enable all three rules (duplicate_item, flag_consistency, zero_item_quality)
- Fixed pre-existing lint issue in tests/test_integrity_rules.py (line too long)
- 20 tests added in `tests/test_zero_item_quality.py` covering applies logic, all warning types, configurable threshold, relative quality, caching, clean items, and engine integration
- Files changed: `app/rules/zero_item_quality.py`, `tests/test_zero_item_quality.py`, `app/tenants/default/tenant.yaml`, `tests/test_integrity_rules.py`
- **Learnings for future iterations:**
  - Pydantic v2 shared_context gotcha still applies — tests must use `ctx.shared_context` not the original dict passed in
  - Relative quality comparison uses Descrição as the grouping key (tipo_item) — this is the closest proxy for item type in canonical fields
  - The threshold comparison uses `<=` (word_count <= threshold) meaning threshold is the max "short" count, not the minimum acceptable
---

## 2026-04-13 - US-006
- Implemented `CategoryCriticalCheckRule` in `app/rules/category_rules.py`
- `classify_category()` matches Descrição against tenant-configured category keywords (case-insensitive substring match)
- Critical checks use regex patterns: `btu_pattern` for air conditioners, `inches_pattern` for TVs
- Searches across Descrição + Complemento + Modelo for pattern matches
- Updated `empresa_exemplo` tenant to enable `category_critical_check` rule
- 20 tests in `tests/test_category_rules.py` covering: classification, applies logic, BTU/inches patterns in various fields, unknown checks, None handling, engine integration
- Files changed: `app/rules/category_rules.py`, `tests/test_category_rules.py`, `app/tenants/empresa_exemplo/tenant.yaml`
- **Learnings for future iterations:**
  - `CRITICAL_CHECK_PATTERNS` dict maps check names to compiled regex — extensible for new category patterns
  - Pattern search spans Descrição + Complemento + Modelo concatenated, so the pattern can appear in any of those fields
  - When using `x or default` with lists, remember empty list `[]` is falsy — use `if x is None` instead
  - Category classification is a simple keyword substring match; no NLP or fuzzy matching needed at this stage
---

## 2026-04-13 - US-007
- Implemented `LLMAuditRule` in `app/rules/llm_audit.py` — optional LLM-based semantic audit for zero-created items
- LLM audit is configurable per tenant via `LLMConfig` (model, temperature, max_tokens, prompt_file)
- Rule only applies when `llm.enabled=true` AND `flag_item_cadastrado_do_zero=1`
- Prompt template loaded from tenant-specific file (e.g., `prompts/audit.txt`), formatted with row fields
- LLM response parsed via regex JSON extraction, normalized into structured `ValidationIssue` findings
- All LLM failures (timeouts, API errors, missing prompts) captured as severity=warning issues, never crash
- `LLMClient` protocol allows easy mocking; `AnthropicLLMClient` wraps the real SDK
- Created prompt template at `app/tenants/empresa_exemplo/prompts/audit.txt`
- Enabled `llm_audit` rule in empresa_exemplo tenant config
- 21 tests in `tests/test_llm_audit.py` covering: applies logic, response parsing, normalization, prompt formatting, validate with mock client, failure handling, prompt not found, engine integration
- Files changed: `app/rules/llm_audit.py`, `tests/test_llm_audit.py`, `app/tenants/empresa_exemplo/prompts/audit.txt`, `app/tenants/empresa_exemplo/tenant.yaml`
- **Learnings for future iterations:**
  - Use Protocol-based client abstraction for external service dependencies to enable easy testing without mocking internals
  - LLM response parsing should be defensive — regex extract JSON array, handle malformed JSON gracefully
  - Prompt files live under tenant directories (e.g., `app/tenants/{tenant_id}/prompts/`) for tenant-specific customization
  - The `anthropic` package is not in dependencies (optional) — AnthropicLLMClient imports it lazily to avoid import errors when LLM is disabled
---

## 2026-04-13 - US-008
- Implemented `JobRecord` model in `app/core/job.py` with full status lifecycle (queued → running → completed/failed)
- `JobStatus` uses `StrEnum` for clean serialization; `JobRecord` persists file_path, result_path, report_path, counters, params, and tenant_id
- State transitions enforced via `transition_to()` with explicit valid-transition map; invalid transitions raise `ValueError`
- Convenience methods: `mark_running()`, `mark_completed()`, `mark_failed()`
- Implemented `JobService` in `app/services/job_service.py` — in-memory job store with create, get, list (filterable by tenant), start, complete, fail
- 26 tests in `tests/test_job.py` covering: default status, ID generation, tenant persistence, file/params persistence, all valid transitions, invalid transitions (completed→running, failed→running, queued→completed), updated_at changes, full lifecycle, service CRUD, tenant filtering, nonexistent job errors
- Files changed: `app/core/job.py`, `app/services/job_service.py`, `tests/test_job.py`
- **Learnings for future iterations:**
  - Use `StrEnum` instead of `str, Enum` inheritance — ruff UP042 enforces this
  - Use `datetime.UTC` alias instead of `timezone.utc` — ruff UP017 enforces this
  - In-memory job storage is sufficient for now; can be swapped for persistent storage later without changing the service interface
  - Job state machine pattern: define valid transitions as a dict, enforce in a single `transition_to()` method
---

## 2026-04-13 - US-009
- Implemented `report_service.py` in `app/services/` with four core functions:
  - `build_row_results()` — structured validation result per row with item, descricao, issues, has_errors, has_warnings
  - `build_duplicate_section()` — groups duplicate items with row indices and counts
  - `build_grouped_problems()` — groups issues by code for operational review
  - `generate_pdf_report()` — produces A4 PDF with summary table, duplicates table, and grouped problems tables using reportlab
- `build_full_report()` composes all sections with a summary (total_rows, rows_with_issues, error/warning counts)
- PDF report includes: title, summary table, duplicate items section, and grouped problems by type (capped at 50 per group)
- 20 tests in `tests/test_report_service.py` covering all functions including empty data, large datasets, string paths
- Files changed: `app/services/report_service.py`, `tests/test_report_service.py`
- **Learnings for future iterations:**
  - reportlab is already in `pyproject.toml` dependencies — no need to add it
  - `SimpleDocTemplate` + `platypus` flowables (Table, Paragraph, Spacer) is the standard pattern for PDF generation
  - Report service is purely functional (no class state) — takes normalized rows + validation results, returns structured data or writes PDF
  - Keep report rendering separate from validation logic — report functions only consume `ValidationIssue` models
---

## 2026-04-13 - US-010
- Implemented validation API with tenant selection in `app/api/routes.py`
- POST `/validate` — accepts file upload + tenant_id query param, creates async validation job via BackgroundTasks
- GET `/jobs/{job_id}` — returns job status with counters and error message
- GET `/jobs/{job_id}/result` — downloads JSON result (requires completed job)
- GET `/jobs/{job_id}/report` — downloads PDF report (requires completed job)
- Created `app/services/validation_service.py` — orchestrates file parsing, engine execution, report generation, and job lifecycle
- `ensure_rules_registered()` lazily registers all rules on first validation run
- Updated `app/main.py` to include the API router
- Added `uploads/` and `results/` to `.gitignore`
- 16 tests in `tests/test_api.py` covering: upload, default tenant, invalid tenant, job status, result/report download (completed/not completed/missing), counters, error messages
- Files changed: `app/api/routes.py`, `app/services/validation_service.py`, `app/main.py`, `.gitignore`, `tests/test_api.py`
- **Learnings for future iterations:**
  - FastAPI `BackgroundTasks` is sufficient for async job processing without external task queues
  - Module-level `JobService()` in routes.py provides shared in-memory state across requests (single process)
  - Rules are not auto-registered at module import — need explicit `ensure_rules_registered()` call before engine use
  - Use `raise HTTPException(...) from None` inside except clauses to satisfy ruff B904
- TestClient executes BackgroundTasks synchronously, which simplifies testing async flows
---

## 2026-04-18 12:52 -03 - US-033
- Implemented a frontend login gate in the Next.js app with explicit `tenant`, `username`, and `password` fields before entering the operational workspace.
- Centralized in-memory frontend auth in `frontend/src/lib/api.ts`, adding `/login` consumption and shared `X-API-Key` injection for authenticated workspace fetches.
- Added focused frontend coverage for login rendering, required-field validation, backend error states, and authenticated header injection.
- Manual browser verification is still needed in a real browser because no browser automation tool is configured in this environment.
- Files changed: `AGENTS.md`, `frontend/package.json`, `frontend/package-lock.json`, `frontend/src/app/globals.css`, `frontend/src/app/page.tsx`, `frontend/src/components/login-screen.tsx`, `frontend/src/components/login-screen.test.tsx`, `frontend/src/lib/api.ts`, `frontend/src/lib/__tests__/api.test.ts`, `frontend/src/lib/types.ts`, `frontend/src/test/setup.ts`, `frontend/vitest.config.ts`
- **Learnings for future iterations:**
  - Frontend login flows should keep the typed tenant input explicit in the UI even when the API already scopes later calls to a single tenant.
  - Keep frontend auth header injection in one place (`frontend/src/lib/api.ts`) so later session persistence/logout work does not require touching each workspace fetch path.
  - Vitest component coverage in `frontend/` now uses `jsdom` + `@testing-library/react`; shared cleanup belongs in `frontend/src/test/setup.ts`.
---

## 2026-04-17 - US-012
- Implemented duplicate CSV consolidation in `app/services/validation_service.py`
- Duplicate resolution now keeps the occurrence with the highest row index, fills missing values from previous duplicate occurrences, removes the other occurrences, and rebuilds the job artifacts in place
- Extended `POST /jobs/{job_id}/duplicates/resolve` to return `merged_columns`
- Updated the operational frontend modal to reflect the automatic highest-row keep rule and show merged column feedback after resolution
- Added focused service and API tests for highest-row duplicate resolution and field merging
- Fixed the legacy integrity-rule integration test to request `ValidationScope.ALL_ITEMS` explicitly, preserving the current default `zero_items` operational scope
- Applied ruff import-order fixes in `app/rules/integrity.py` and `app/services/report_service.py`

- Updated `prd.json` with US-012 for duplicate consolidation
- Deployed:
  - API image: `ttl.sh/validador-final-api-20260417104643:24h`
  - Frontend image: `ttl.sh/validador-final-frontend-20260417104643:24h`
  - Namespace: `validador-final`
  - Frontend URL: `http://192.168.0.20:30092`
  - API URL: `http://192.168.0.20:30091`
- Checks passed:
  - `ruff check app tests`
  - `pytest`
  - `cd frontend && npm run typecheck`
  - `cd frontend && npm run test`
  - `cd frontend && npm run lint`
- **Learnings for future iterations:**
  - Duplicate consolidation belongs in the CSV correction service, not in row validation rules, because it mutates the corrected artifact and triggers job artifact regeneration
  - Keep duplicate merge behavior generic: operate on row indices and CSV values without tenant-name branching
  - `ttl.sh` images expire after 24h; use a persistent registry for durable deployments
---

## 2026-04-17 - US-013
- Fixed job loss during duplicate resolution after API pod restart or rollout
- Added optional JSON-backed persistence to `JobService`, enabled by `VALIDATOR_JOB_STORE_PATH`
- API upload and reprocess flows now persist direct `file_path`/`file_name` assignments before background work continues
- Updated the API Kubernetes deployment to mount `uploads` and `results` through PVCs instead of `emptyDir`
- Added `VALIDATOR_JOB_STORE_PATH=/app/results/jobs.json` to the API deployment
- Added job service tests for reloading persisted jobs, lifecycle persistence, and explicit persistence after external file-path assignment
- Deployed:
  - API image: `ttl.sh/validador-final-api-20260417111500:24h`
  - PVCs: `validador-final-uploads`, `validador-final-results`
  - Storage class: `nfs-client`
- Validation performed:
  - Uploaded a duplicate test CSV through the deployed API
  - Confirmed the job completed with one duplicate group
  - Restarted `deployment/validador-final`
  - Confirmed the same completed job was still available after restart
  - Resolved duplicates after restart and confirmed merged columns plus zero duplicate groups
- Checks passed:
  - `ruff check app tests`
  - `pytest`
  - `cd frontend && npm run typecheck`
  - `cd frontend && npm run test`
  - `cd frontend && npm run lint`
- **Learnings for future iterations:**
  - In-memory job state is not acceptable for operational correction flows because the UI can hold a job id across backend rollouts
  - Job metadata and CSV/result artifacts must move together; persisting only one side still leaves duplicate resolution fragile
- Jobs created before this fix while the API used `emptyDir` cannot be recovered after that pod is terminated
---

## 2026-04-17 - US-014
- Expanded duplicate-resolution media extraction in `frontend/src/lib/row-media.ts` to preserve every renderable image source from raw row payloads while keeping first-photo compatibility helpers
- Added focused media parsing tests for mixed HTTPS/local-path lists, `data:image` payloads, raw base64 payloads, and multi-column deduplication in `frontend/src/lib/__tests__/row-media.test.ts`
- Reworked the duplicate-resolution modal cards to show a stable in-card photo gallery with horizontal scroll, discrete previous/next controls, load-error fallback, and responsive layout preservation
- Added a frontend lightbox for duplicate-row photos with same-row navigation and keyboard support in `frontend/src/components/duplicate-resolution-modal.tsx`
- Kept the implementation frontend-only; the existing `GET /jobs/{job_id}/rows/{row_index}` row payload was sufficient and no API/backend change was required
- Updated `prd.json` and `TASKS.md` for the duplicate photo gallery rollout
- Deployed:
  - Frontend image: `ttl.sh/validador-final-frontend-20260417125614:24h`
  - Namespace: `validador-final`
  - Frontend URL: `http://192.168.0.20:30092`
  - API URL reused unchanged: `http://192.168.0.20:30091`
- Validation performed:
  - Confirmed `deployment/validador-final-frontend` rolled out successfully with the new image
  - Confirmed `GET http://192.168.0.20:30092` returned `HTTP/1.1 200 OK`
  - Confirmed `GET http://127.0.0.1:30091/health` returned `{"status":"ok"}`

- Checks passed:
  - `cd frontend && npm run typecheck`
  - `cd frontend && npm run test`
  - `cd frontend && npm run lint`
- **Learnings for future iterations:**
  - Real Redesim `foto_complementar_memento` cells can contain several HTTPS URLs in a textual list; the example dataset observed up to 7 browser-renderable URLs in one cell
  - When a media cell mixes HTTPS URLs with Android-local paths, only the browser-renderable sources should participate in gallery/lightbox navigation and the rest should be silently ignored
---

## 2026-04-17 - US-015
- Added a duplicate-result filter in `frontend/src/components/result-workspace.tsx` with `Todos`, `Nomes iguais`, and `Nomes diferentes` views plus per-filter counts
- Moved duplicate conflict detection and filtering into shared presentation helpers in `frontend/src/lib/presentation.ts` so red-card styling and the new filter use the same conflict rule
- Added focused frontend tests for explicit duplicate conflict detection and filter partitioning in `frontend/src/lib/__tests__/presentation.test.ts`
- Added frontend styles for the duplicate filter chips and empty filtered state in `frontend/src/app/globals.css`
- Reset the duplicate filter to `Todos` whenever the operator opens a different job to avoid carrying stale UI state across lots
- Renamed the operator-facing filter labels from generic severity wording to comparison wording so the UI now speaks in terms of `Nomes iguais` and `Nomes diferentes`
- Updated `prd.json` and `TASKS.md` for the duplicate filter rollout
- Deployed:
  - Frontend image: `ttl.sh/validador-final-frontend-20260417144230:24h`
  - Namespace: `validador-final`
  - Frontend URL: `http://192.168.0.20:30092`
  - API URL reused unchanged: `http://192.168.0.20:30091`
- Validation performed:
  - Confirmed `deployment/validador-final-frontend` rolled out successfully with the new image
  - Confirmed `GET http://192.168.0.20:30092` returned `HTTP/1.1 200 OK`
  - Confirmed `GET http://127.0.0.1:30091/health` returned `{"status":"ok"}`
- Checks passed:
  - `cd frontend && npm run typecheck`
  - `cd frontend && npm run test`
  - `cd frontend && npm run lint`
- **Learnings for future iterations:**
  - The duplicate list can be filtered entirely on the frontend because the backend already returns enough duplicate metadata (`has_description_conflict`, `descricao`, `row_indices`, `count`) to classify cards without extra API work
  - UI filters that derive from operational severity should default back to the broadest view when the active job changes, otherwise operators can miss items because of stale local state
---

## 2026-04-17 - US-016
- Updated duplicate consolidation in `app/services/validation_service.py` to detect `Nomes iguais` groups through the current duplicate description rule and keep the physical highest-row survivor only as a CSV persistence detail
- Same-name duplicate enrichment now ignores photo/media columns and date/time-like columns before filling missing values from older occurrences
- Preserved the current resolution behavior for `Nomes diferentes` duplicate groups
- Added focused service and API tests covering same-name consolidation with media columns, explicit date/time columns, and a value-based datetime fallback column (`registro`)
- Updated duplicate-resolution copy in `frontend/src/components/duplicate-resolution-modal.tsx`, `frontend/src/components/operational-workspace.tsx`, and `frontend/src/components/result-workspace.tsx` to speak in terms of consolidation/mescla instead of “manter maior linha”
- Conflict rule kept intentionally conservative for `Nomes iguais`: if the surviving row already has a non-empty allowed value, that value wins; older occurrences only fill blanks
- Updated `prd.json` and `TASKS.md` for the same-name consolidation rollout
- Deployed:
  - API image: `ttl.sh/validador-final-api-20260417153018:24h`
  - Frontend image: `ttl.sh/validador-final-frontend-20260417153018:24h`
  - Namespace: `validador-final`
  - Frontend URL: `http://192.168.0.20:30092`
  - API URL: `http://192.168.0.20:30091`
- Validation performed:
  - Confirmed `deployment/validador-final` rolled out successfully with the new API image
  - Confirmed `deployment/validador-final-frontend` rolled out successfully with the new frontend image
  - Confirmed `GET http://192.168.0.20:30092` returned `HTTP/1.1 200 OK`
  - Confirmed `GET http://127.0.0.1:30091/health` returned `{"status":"ok"}`
- Checks passed:
  - `ruff check app/services/validation_service.py tests/test_validation_service.py tests/test_api.py`
  - `pytest tests/test_validation_service.py tests/test_api.py`
  - `ruff check app tests`
  - `pytest`
  - `cd frontend && npm run typecheck`
  - `cd frontend && npm run test`
  - `cd frontend && npm run lint`
- **Learnings for future iterations:**
  - The current `Nomes iguais` vs `Nomes diferentes` split still depends on the existing duplicate-description rule: one distinct non-empty `Descrição` means same-name; more than one means different-name
  - Date/time skip detection is safest when combining normalized column-name tokens with a small sample of datetime-like values, because real tenant files may not use stable headers for timestamps
  - When the raw CSV cannot resolve the `Descrição` source column during duplicate resolution, falling back to the existing different-name path is the safer default than applying the stricter same-name merge rules
---

## 2026-04-17 - US-017
- Added a mass-consolidation shortcut in `frontend/src/components/result-workspace.tsx` for duplicate groups classified as `Nomes iguais`
- Kept the bulk shortcut intentionally limited to groups with exactly 2 occurrences for now
- Added shared helper logic in `frontend/src/lib/presentation.ts` to identify eligible groups and sort them from the highest row indices to the lowest before the UI invokes the existing per-group resolution API
- Updated `frontend/src/components/operational-workspace.tsx` to execute the eligible consolidations sequentially, refresh the completed job once at the end, and surface partial-progress errors if a batch fails mid-run
- Added focused frontend coverage in `frontend/src/lib/__tests__/presentation.test.ts` for bulk eligibility and descending ordering
- Updated `prd.json` and `TASKS.md` for the bulk same-name consolidation shortcut
- Deployed:
  - Frontend image: `ttl.sh/validador-final-frontend-20260417154751:24h`
  - Namespace: `validador-final`
  - Frontend URL: `http://192.168.0.20:30092`
  - API URL reused unchanged: `http://192.168.0.20:30091`
- Validation performed:
  - Confirmed `deployment/validador-final-frontend` rolled out successfully with the new image
  - Confirmed `GET http://192.168.0.20:30092` returned `HTTP/1.1 200 OK`
  - Confirmed `GET http://127.0.0.1:30091/health` returned `{"status":"ok"}`
- Checks passed:
  - `ruff check app tests`
  - `pytest`
  - `cd frontend && npm run typecheck`
  - `cd frontend && npm run test`
  - `cd frontend && npm run lint`
- **Learnings for future iterations:**
  - Reusing the existing duplicate-resolution endpoint is safe for bulk actions as long as eligible groups are processed from the end of the CSV toward the beginning
  - Keeping the eligibility rule in a shared presentation helper avoids subtle drift between the button visibility, the count shown in the UI, and the actual groups sent to the API
---

## 2026-04-18 - US-011
- Closed the remaining acceptance gap in US-011 ("Test flag derivation from Placa Anterior") by adding a focused test module for canonical-field flag derivation
- Added `tests/test_canonical_fields.py` with unit coverage for `derive_flags()` across present/None/empty/whitespace placa values, mutual-exclusivity of the derived flags, and `normalize_row()` flag propagation including tenant column mapping
- Confirmed the other US-011 criteria were already covered: tenant loading (`tests/test_tenant.py`), duplicate Item validation (`tests/test_integrity_rules.py`), zero-item rules (`tests/test_zero_item_quality.py`), category critical checks (`tests/test_category_rules.py`), job creation and state transitions (`tests/test_job.py`)
- Files changed: `tests/test_canonical_fields.py`, `prd.json`, `progress.txt`
- Checks passed:
  - `ruff check app tests`
  - `pytest` (263 tests)
- **Learnings for future iterations:**
  - `derive_flags()` was only exercised indirectly through `normalize_row()` — acceptance criteria that name a specific helper deserve a direct unit test even when broader coverage exists
  - Treat "flags are mutually exclusive" as a property-style assertion to catch regressions if the placa-presence semantics ever shift
  - US-011 is behavior-focused coverage, not new functionality: it locks in existing domain semantics against future tenant-specific changes
---

## 2026-04-18 - US-018
- Added central JSON structured logger at `app/core/logging.py` with standardized fields (`ts`, `level`, `event`, `tenant_id`, `job_id`, `request_id`, `duration_ms`, `error`), a `configure_logging()` entrypoint driven by `LOG_LEVEL` and `LOG_FORMAT` env vars, and a `log_event()` helper that every layer reuses without coupling to FastAPI or the job store
- Added request-id contextvar and `app/api/middleware.py` injecting `X-Request-ID` per request; echoes the incoming header back or generates a uuid4 when absent, and flows the id into the logger context so service/worker logs correlate with the request
- Wired the middleware and `configure_logging()` into `app/main.py`
- Instrumented `JobService` lifecycle (`job.created`, `job.started`, `job.completed`, `job.failed`, `job.canceled`) with tenant/job ids and duration measured from creation; `run_validation_job` now emits `validation.started`, `validation.completed`, `validation.failed`, and `validation.canceled` with duration from the background-task entry point
- Added focused tests in `tests/test_logging.py` covering env-driven level/format, JSON and text formatters, `log_event` field propagation, request-id middleware round-trip (incoming header vs. auto-generated), and lifecycle event emission from both `JobService` and the async validation pipeline
- Files changed: `app/core/logging.py`, `app/api/middleware.py`, `app/main.py`, `app/services/job_service.py`, `app/services/validation_service.py`, `tests/test_logging.py`, `prd.json`, `progress.txt`
- Checks passed:
  - `ruff check app tests`
  - `pytest` (281 tests)
- **Learnings for future iterations:**
  - The structured logger uses its own `validator` namespace with `propagate=False`, so pytest's `caplog` will not capture records by default; attach a recording handler directly to the `validator` logger (and flip `_configured=True`) to intercept events in tests
  - FastAPI `StreamHandler(stream=sys.stdout)` is captured by pytest's `capsys` as long as the handler is constructed after the fixture has swapped `sys.stdout`, so call `configure_logging(force=True)` inside the test body
  - Keeping the logger as a thin function (`log_event`) avoids leaking a logging framework into rules, engines, or report code — the pattern is the expected extension point for future metrics/audit stories
---

## 2026-04-18 10:14 -03 - US-019
- Added centralized Prometheus instrumentation in `app/core/metrics.py` with a dedicated registry, opt-in gating via `VALIDATOR_METRICS_ENABLED`, job lifecycle counters/histograms, rule execution and issue counters, and LLM request latency/failure metrics
- Exposed `/metrics` in `app/main.py` while keeping full disablement via 404 when metrics are not enabled
- Instrumented `ValidationEngine`, `JobService`, and `LLMAuditRule` through metrics helpers instead of direct Prometheus calls, preserving thin API and reusable core/service boundaries
- Added focused smoke coverage in `tests/test_metrics.py` for disabled-by-default behavior, Prometheus payload exposure when enabled, and LLM failure metrics
- Files changed: `app/core/metrics.py`, `app/core/engine.py`, `app/main.py`, `app/rules/llm_audit.py`, `app/services/job_service.py`, `tests/test_metrics.py`, `pyproject.toml`, `AGENTS.md`, `prd.json`, `progress.txt`
- Checks passed:
  - `ruff check app tests`
  - `pytest` (284 tests)
- **Learnings for future iterations:**
  - Use a dedicated Prometheus `CollectorRegistry` in `app/core/metrics.py` so tests can reset instrumentation cleanly without colliding with the process-global default registry
  - Keep metric labels low-cardinality and tenant-agnostic: `tenant_id`, `rule`, `severity`, `status`, and bounded `outcome` values are acceptable; `job_id` and `request_id` are not
  - LLM latency/failure metrics belong inside `LLMAuditRule`, while rule-level execution and issue counts belong in the generic engine so non-LLM rules inherit observability automatically
---

## 2026-04-18 10:25 -03 - US-020
- Implemented operational health probing in `app/core/health.py` and changed `/health` in `app/main.py` to return a global status plus structured checks with `name`, `ok`, `latency_ms`, and optional `detail`
- Added critical storage probes for uploads, results, and the optional JSON job store; critical failures now return HTTP 503 while optional failures can degrade the payload without taking the endpoint down
- Added conservative LLM reachability probing through `app/rules/llm_audit.py`, exposed as an opt-in per-tenant flag `llm.healthcheck_enabled` in `app/core/tenant_config.py`
- Exposed `JobService.storage_path` for health inspection and updated seed tenant examples plus focused health/API tests
- Files changed: `app/core/health.py`, `app/core/tenant_config.py`, `app/main.py`, `app/rules/llm_audit.py`, `app/services/job_service.py`, `app/tenants/default/tenant.yaml`, `app/tenants/empresa_exemplo/tenant.yaml`, `tests/test_health.py`, `tests/test_api.py`, `app/tests/test_health.py`, `AGENTS.md`, `prd.json`, `progress.txt`
- Checks passed:
  - `ruff check app tests`
  - `pytest` (287 tests)
- **Learnings for future iterations:**
  - Keep LLM health checks separate from LLM audit execution: a socket-level provider reachability probe is enough for `/health` and avoids unnecessary token spend
  - Resolve mutable runtime dependencies inside the route path (for example, `job_service` from `app.api.routes`) instead of capturing them by value in `app.main`, otherwise tests and future storage swaps become harder
  - When a config contract grows for an operational feature, update the seed tenant YAMLs even if the new field defaults safely; it makes the intended opt-in posture explicit
---

## 2026-04-18 10:36 -03 - US-021
- Implemented tenant-scoped API key authentication with `X-API-Key`, deriving tenant context in middleware and rejecting request `tenant_id` hints that do not match the authenticated tenant
- Restricted tenant listing, job listing, job status, artifact downloads, row editing, duplicate resolution, cancellation, and reprocessing to the authenticated tenant while keeping `/health`, `/metrics`, and `OPTIONS` preflight public
- Added `api_keys` support to tenant configs and seed tenant YAMLs, plus focused coverage for missing key, invalid key, tenant mismatch, scoped job access, and tenant API-key resolution
- Files changed: `app/api/auth.py`, `app/api/routes.py`, `app/core/tenant_config.py`, `app/core/tenant_loader.py`, `app/main.py`, `app/tenants/default/tenant.yaml`, `app/tenants/empresa_exemplo/tenant.yaml`, `app/tenants/redesim/tenant.yaml`, `app/tenants/redesim_v2/tenant.yaml`, `tests/test_api.py`, `tests/test_logging.py`, `tests/test_metrics.py`, `tests/test_tenant.py`, `AGENTS.md`
- Checks passed:
  - `ruff check --fix app tests`
  - `pytest` (295 tests)
- **Learnings for future iterations:**
  - Middleware-authenticated tenant context should be the source of truth for API authorization; routes should only compare request hints or job ownership against that context, not reload tenant selection logic ad hoc
  - If API auth depends on a custom header such as `X-API-Key`, unauthenticated `OPTIONS` requests must remain allowed so browser CORS preflight does not break uploads or PATCH flows
  - Keeping tenant API keys in `tenant.yaml` as `{key_id, value}` pairs preserves a working default tenant today and leaves the loader as the only place that needs to change if secrets move to an external store later
---

## 2026-04-18 10:44 -03 - US-022
- Implemented tenant-scoped artifact path helpers in `app/services/validation_service.py` for uploads, results, and reports
- Updated API upload/download flows to persist new job paths under `uploads/{tenant_id}` and `results/{tenant_id}`, while keeping legacy root-path fallback reads for older jobs
- Added focused backend and API tests covering tenant-scoped artifact creation for multiple tenants and legacy fallback reads for CSV/result/report artifacts
- Files changed: `AGENTS.md`, `app/api/routes.py`, `app/services/validation_service.py`, `tests/test_api.py`, `tests/test_validation_service.py`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Keep artifact path derivation centralized so tenant isolation changes do not leak into routes or job callers
  - When migrating on-disk layouts, preserve read fallback for one cycle so persisted legacy jobs remain operable during rollout
  - Cover upload path assertions at the API layer because the initial CSV path is assigned before the validation worker runs
---

## 2026-04-18 10:54 -03 - US-023
- Implemented persistent audit events with `AuditEvent`/`AuditService`, including JSON-backed storage compatible with reloads and tenant-scoped listing
- Wired audit emission into job creation/completion and duplicate resolution, preserving `api_key_id` as metadata without storing the raw API key secret
- Exposed authenticated `GET /audit` with tenant hint validation and limit support
- Added focused tests for audit persistence/filtering plus API coverage for listing and emitted events
- Files changed: `AGENTS.md`, `app/api/routes.py`, `app/core/audit.py`, `app/core/job.py`, `app/services/audit_service.py`, `app/services/job_service.py`, `app/services/validation_service.py`, `tests/test_api.py`, `tests/test_audit.py`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Keep operational audit persistence parallel to `JobService`: optional JSON-backed storage is enough for single-node retention without coupling audit reads to job internals
  - Capture `api_key_id` at job creation time if background completion events need to preserve who initiated the run after request context is gone
  - Audit endpoints should reuse the authenticated tenant context and only accept `tenant_id` as a scoped hint, matching the rest of the API
---

## 2026-04-18 11:02 -03 - US-024
- Implemented tenant-configured Marca/Modelo normalization with typed `NormalizationConfig` in `app/core/tenant_config.py`
- Added `app/core/normalization/` with a reusable `BrandModelNormalizer` that resolves exact aliases case- and accent-insensitively
- Applied normalization in the generic row-normalization pipeline before rules and preserved pre-normalized source values in `ValidationContext.raw_row`
- Updated seed tenant YAMLs to expose the new config contract and added focused tests for config loading, alias matching, fallback behavior, missing dictionaries, and engine integration
- Files changed: `AGENTS.md`, `app/core/canonical_fields.py`, `app/core/context.py`, `app/core/engine.py`, `app/core/normalization/__init__.py`, `app/core/normalization/brand_model.py`, `app/core/tenant_config.py`, `app/services/validation_service.py`, `app/tenants/default/tenant.yaml`, `app/tenants/empresa_exemplo/tenant.yaml`, `tests/test_engine.py`, `tests/test_normalization.py`, `tests/test_tenant.py`, `prd.json`, `progress.txt`
- Checks passed:
  - `ruff check --fix app tests`
  - `pytest` (311 tests)
- **Learnings for future iterations:**
  - Keep tenant alias dictionaries in typed config (`tenant.normalization`) so future normalization features can reuse the same contract without adding tenant conditionals to the core
  - Preserve raw source values in `ValidationContext.raw_row` whenever canonical normalization changes a field; this keeps future rules and audit/report features able to compare canonical vs uploaded values
  - Exact alias normalization should stay conservative for now: casefold + accent removal + whitespace normalization is enough for deterministic matching, while fuzzy matching remains a separate concern
---

## 2026-04-18 11:08 -03 - US-025
- Implemented `BrandModelConsistencyRule` in `app/rules/brand_model_consistency.py` to warn when a row's `Marca` diverges from the canonical brand inferred from the tenant-configured model dictionary
- Extended `tenant.normalization` with `model_brands`, added reusable `BrandModelNormalizer.infer_brand_for_model()`, and registered the new rule in `ensure_rules_registered()`
- Updated seed tenant examples and focused tests covering config loading, canonical/alias model lookup, rule match/mismatch/silent behavior, and engine integration after model normalization
- Files changed: `AGENTS.md`, `app/core/normalization/brand_model.py`, `app/core/tenant_config.py`, `app/rules/brand_model_consistency.py`, `app/services/validation_service.py`, `app/tenants/default/tenant.yaml`, `app/tenants/empresa_exemplo/tenant.yaml`, `tests/test_brand_model_consistency.py`, `tests/test_normalization.py`, `tests/test_tenant.py`, `prd.json`, `progress.txt`
- Checks passed:
  - `ruff check app tests`
  - `pytest` (315 tests)
- **Learnings for future iterations:**
  - Keep `model_brands` keyed by canonical model names and let the normalizer project aliases onto that lookup; this avoids duplicating alias spellings across tenant dictionaries
  - Brand/model consistency should compare against normalized canonical row values, not `raw_row`, so alias normalization and future tenant dictionaries compose cleanly
  - Keep the rule opt-in and silent when the model is not dicionarized or the brand is blank; missing-brand coverage belongs to the existing quality rules, while this rule should stay focused on inconsistencies
---

## 2026-04-18 11:15 -03 - US-026
- Implemented `SuspiciousPatternRule` in `app/rules/suspicious_patterns.py` to detect tenant-configured suspicious literals and regexes in canonical `descricao` and `complemento`
- Added typed tenant config `SuspiciousPatternsConfig` in `app/core/tenant_config.py` and validated regex entries during config loading
- Registered the new rule in `app/services/validation_service.py` and updated the default tenant example with an empty `suspicious_patterns` section
- Added focused tests in `tests/test_suspicious_patterns.py` and expanded `tests/test_tenant.py` coverage for the new config defaults and parsing
- Files changed: `app/core/tenant_config.py`, `app/rules/suspicious_patterns.py`, `app/services/validation_service.py`, `app/tenants/default/tenant.yaml`, `tests/test_tenant.py`, `tests/test_suspicious_patterns.py`
- **Learnings for future iterations:**
  - Regex-backed tenant config should be validated when the tenant is loaded, not lazily during rule execution
  - Suspicious text detection should stay opt-in and operate over normalized canonical fields rather than raw upload parsing
---

## 2026-04-18 11:26 -03 - US-027
- Implemented versioned LLM prompt loading with optional YAML frontmatter, defaulting legacy prompts to `legacy`
- Added structured `meta` support to `ValidationIssue` and propagated `prompt_version` plus `model` through LLM findings/failures and final report artifacts
- Included LLM prompt/model metadata in the structured job result and PDF identity panel, even when the audit returns no findings, by recording job-level LLM metadata in shared validation context
- Updated the example tenant prompt with an explicit version and added focused tests for frontmatter parsing, metadata propagation, and final result/PDF reporting
- Files changed: `AGENTS.md`, `app/core/issue.py`, `app/rules/llm_audit.py`, `app/services/report_service.py`, `app/services/validation_service.py`, `app/tenants/empresa_exemplo/prompts/audit.txt`, `tests/test_llm_audit.py`, `tests/test_report_service.py`, `tests/test_validation_service.py`, `prd.json`, `progress.txt`
- Checks passed:
  - `ruff check app tests`
  - `pytest` (325 tests)
- **Learnings for future iterations:**
  - Parse prompt version metadata where prompt files are loaded, not inside report generation, so legacy/default handling stays centralized and future cache keys can reuse the same contract
  - Carry LLM audit provenance through `ValidationIssue.meta` for row-level consumers, but also mirror job-level LLM metadata in `shared_context` so final reports still know the prompt version when the LLM returns no findings
  - Report metadata should be derived in the service/report layer from structured issue/shared-context data rather than hardcoded route-level fields
---

## 2026-04-18 11:44 -03 - US-028
- Implemented persistent JSON-backed LLM response caching keyed by tenant_id, prompt_version, model, and normalized audit payload
- Added tenant-configured TTL via `llm.cache_ttl_seconds`; `0` keeps caching disabled, while positive values enable local cache reuse
- Added `force_refresh` job/API param support so validation and reprocess jobs can bypass cache reads while storing the refreshed successful response
- Added opt-in Prometheus cache metrics through centralized helpers with bounded outcomes (`hit`, `miss`, `bypass`)
- Files changed: `app/core/llm_cache.py`, `app/core/metrics.py`, `app/core/tenant_config.py`, `app/rules/llm_audit.py`, `app/services/validation_service.py`, `app/api/routes.py`, `app/tenants/default/tenant.yaml`, `app/tenants/empresa_exemplo/tenant.yaml`, `tests/test_llm_cache.py`, `tests/test_llm_audit.py`, `tests/test_tenant.py`, `tests/test_metrics.py`, `tests/test_validation_service.py`, `tests/test_api.py`, `AGENTS.md`, `prd.json`, `progress.txt`
- Checks passed:
  - `ruff check --fix app tests`
  - `pytest tests/test_llm_cache.py tests/test_llm_audit.py tests/test_tenant.py tests/test_metrics.py tests/test_validation_service.py::test_run_validation_job_records_llm_prompt_version_in_result_and_pdf tests/test_api.py::test_upload_can_request_llm_force_refresh tests/test_api.py::test_reprocess_job_can_override_llm_force_refresh`
  - `ruff check app tests`
  - `pytest` (335 tests)
- **Learnings for future iterations:**
  - Cache storage should stay outside the LLM rule in `app/core/llm_cache.py`; the rule only builds keys, reads/writes successful responses, and preserves existing failure warnings
  - Keep cache metrics centralized in `app/core/metrics.py` with bounded labels and no per-job identifiers, matching the existing observability contract
  - Set `VALIDATOR_LLM_CACHE_PATH` in tests that exercise real tenant LLM flows so cache files do not leak into the default `results/` tree
---

## 2026-04-18 11:52 -03 - US-029
- Added tenant-configured LLM fallback support through `llm.fallback_model` while preserving the existing primary-model behavior when no fallback is configured
- Updated `LLMAuditRule` to retry the fallback model only for transient failures: timeout-like errors, HTTP 429, and HTTP 5xx
- Failure warnings now include the attempted model sequence plus primary and final failure details in structured issue metadata
- Preserved LLM cache semantics per model: cache hits short-circuit a model attempt, successful fallback responses are stored under the fallback model key, and `force_refresh` still bypasses reads while refreshing successful writes
- Files changed: `app/core/tenant_config.py`, `app/rules/llm_audit.py`, `app/tenants/default/tenant.yaml`, `app/tenants/empresa_exemplo/tenant.yaml`, `app/tenants/redesim/tenant.yaml`, `app/tenants/redesim_v2/tenant.yaml`, `tests/test_llm_audit.py`, `tests/test_tenant.py`, `AGENTS.md`, `prd.json`, `progress.txt`
- Checks passed:
  - `ruff check app/rules/llm_audit.py app/core/tenant_config.py tests/test_llm_audit.py tests/test_tenant.py`
  - `pytest tests/test_llm_audit.py tests/test_tenant.py`
  - `ruff check app tests`
  - `pytest` (341 tests)
- **Learnings for future iterations:**
  - Keep LLM fallback policy explicit and narrow so configuration does not turn every provider/client error into a duplicate request
  - Cache keys already include model, so fallback cache behavior composes naturally when the attempted model is passed through the existing key builder
  - Failure issue metadata is the right place to preserve attempted model order and primary/final failure reasons; reports and downstream tools can read structured details without parsing message text
---

## 2026-04-18 12:00 -03 - US-030
- Added debounced text search to the result workspace, filtering visible duplicate cards and problem occurrence rows without changing the backend/API contract
- Added normalized case- and accent-insensitive search helpers in `frontend/src/lib/presentation.ts`, including duplicate-filter combination and problem-group filtering over the existing result payload
- Reset result search input and debounced query whenever `currentJobId` changes, alongside the existing duplicate filter reset
- Added focused frontend coverage for normalized matching, reset state, and combining search with duplicate display filters
- Files changed: `frontend/src/components/result-workspace.tsx`, `frontend/src/lib/presentation.ts`, `frontend/src/lib/__tests__/presentation.test.ts`, `frontend/src/app/globals.css`, `AGENTS.md`, `prd.json`, `progress.txt`
- Checks passed:
  - `cd frontend && npm run typecheck`
  - `cd frontend && npm run test`
  - `cd frontend && npm run lint`
- Browser verification: no browser-testing connector was available in this session; manual UI verification is still recommended before deployment.
- **Learnings for future iterations:**
  - Result workspace search can stay frontend-only because duplicate groups and grouped problem occurrences already carry enough text metadata for operational filtering
  - Keep search normalization in shared presentation helpers so counts, duplicate cards, and problem rows use the same matching semantics
  - Debounced search state should be cleared on job changes together with other local workspace filters to avoid stale filtered views
---

## 2026-04-18 12:10 -03 - US-031
- Added combinable result filters in the operational frontend for priority/severity, validation rule, and item category
- Kept filter derivation and AND semantics in `frontend/src/lib/presentation.ts`; `ResultWorkspace` now owns only local state, reset, and rendering
- Category chips are derived from existing `CATEGORY_*` grouped problem codes, preserving the current backend/API payload
- Filter chips show counts recalculated against the other active filters and disappear for dimensions with no data
- Reset result filters when `currentJobId` changes so stale result views do not carry across jobs
- Added focused frontend coverage for filter combination, coherent counts, no-category payloads, and reset/toggle behavior
- Updated root `AGENTS.md` with the reusable frontend category-filter convention
- Files changed: `frontend/src/lib/presentation.ts`, `frontend/src/components/result-workspace.tsx`, `frontend/src/app/globals.css`, `frontend/src/lib/__tests__/presentation.test.ts`, `AGENTS.md`, `prd.json`, `progress.txt`
- Checks passed:
  - `cd frontend && npm run typecheck`
  - `cd frontend && npm run test`
  - `cd frontend && npm run lint`
  - `ruff check app tests`
  - `pytest` (341 tests)
- Browser verification: no browser-testing connector was available in this session; manual UI verification is still recommended before deployment.
- **Learnings for future iterations:**
  - Keep result filters as facet-style helpers: each chip count should apply the other active filter dimensions so operators can see what a click would expose
  - The current result payload does not expose a row category field, so category filtering should stay conservative and use only category-backed issue codes for now
  - Reset search, duplicate filters, and result filters together on job changes; splitting those resets makes stale filtered views easy to reintroduce
---

## 2026-04-18 12:25 -03 - US-032
- Implemented persistent row review markers for completed job results with `PATCH /jobs/{job_id}/rows/{row_index}/flag`, accepting `review` and `clear`
- Stored mutable review state in a tenant-scoped sidecar at `results/{tenant_id}/{job_id}/review_flags.json` and overlaid `review_flags` into `GET /jobs/{job_id}/result`
- Added frontend marker controls per problem row and a `Para revisão` filter that composes with search, duplicate filters, and result facets
- Added focused backend tests for flag/unflag persistence and reload survival, plus API coverage for mark/clear and invalid row handling
- Added focused frontend presentation tests for review marker counts, problem filtering, and duplicate filtering with review flags
- Updated root `AGENTS.md` with the reusable sidecar/overlay convention for mutable result annotations
- Files changed: `app/services/validation_service.py`, `app/api/routes.py`, `tests/test_validation_service.py`, `tests/test_api.py`, `frontend/src/lib/types.ts`, `frontend/src/lib/api.ts`, `frontend/src/lib/presentation.ts`, `frontend/src/components/operational-workspace.tsx`, `frontend/src/components/result-workspace.tsx`, `frontend/src/app/globals.css`, `frontend/src/lib/__tests__/presentation.test.ts`, `AGENTS.md`, `prd.json`, `progress.txt`
- Checks passed:
  - `ruff check app tests`
  - `pytest` (345 tests)
  - `cd frontend && npm run typecheck`
  - `cd frontend && npm run test` (30 tests)
  - `cd frontend && npm run lint`
- Browser verification: no browser-testing connector was available in this session; manual UI verification is still recommended before deployment.
- **Learnings for future iterations:**
  - Mutable operator state should stay in sidecar files and be merged into read payloads so reprocessing/report generation can keep immutable artifacts stable
  - Review flags should be validated against rows present in the current result payload to avoid stale or out-of-scope row markers
  - Frontend review filtering composes cleanly when `review_flags` is treated as another presentation-layer filter, not a backend query
---

## 2026-04-18 12:34 -03 - US-021
- Implemented tenant-scoped login-issued API keys with a public `POST /login` endpoint that accepts tenant, username, and password and returns an `X-API-Key` value scoped to the authenticated tenant/operator
- Added `OperatorConfig` to tenant configuration and seeded default, empresa_exemplo, redesim, and redesim_v2 tenants with PBKDF2 password hashes instead of plaintext operator passwords
- Added `AuthService` with optional JSON-backed key persistence via `VALIDATOR_API_KEY_STORE_PATH`; issued API keys are generated with high-entropy random values and persisted only as SHA-256 hashes with tenant/operator metadata
- Updated auth middleware to resolve issued keys first, preserve the legacy tenant `api_keys` fallback, keep `/login`, `/health`, `/metrics`, docs, and preflight public, and continue rejecting mismatched tenant hints with 403
- Added focused tests for password verification, secure issued-key persistence/reload, successful login, invalid credentials, unknown tenant, wrong-tenant operator, tenant hint conflict, and job list scoping by issued key
- Files changed: `app/core/tenant_config.py`, `app/services/auth_service.py`, `app/api/auth.py`, `app/api/routes.py`, `app/tenants/default/tenant.yaml`, `app/tenants/empresa_exemplo/tenant.yaml`, `app/tenants/redesim/tenant.yaml`, `app/tenants/redesim_v2/tenant.yaml`, `tests/test_auth_service.py`, `tests/test_api.py`, `tests/test_tenant.py`, `AGENTS.md`, `prd.json`, `progress.txt`
- Checks passed:
  - `ruff check app tests`
  - `pytest tests/test_auth_service.py tests/test_api.py tests/test_tenant.py` (83 tests)
  - `pytest` (357 tests)
- **Learnings for future iterations:**
  - Treat tenant operator credentials as tenant config metadata, but keep runtime issued-key state in `AuthService` so middleware and routes share one resolver
  - Store only issued API key hashes; the raw key should appear only in the login response and request header
  - Keeping the legacy `api_keys` fallback avoids breaking existing API tests and deployments while the login-issued key flow is adopted by the frontend
---

## 2026-04-18 12:55 -03 - US-034
- Validated that the existing backend login flow already satisfies the story acceptance criteria for authenticated `X-API-Key` issuance per tenant/operator
- Confirmed coverage in `app/api/routes.py`, `app/services/auth_service.py`, `app/api/auth.py`, `tests/test_auth_service.py`, and `tests/test_api.py`; no additional backend code change was required
- Updated `prd.json` to mark `US-034` as passed based on the shipped `/login` + issued-key implementation
- Files changed: `prd.json`, `progress.txt`
- Checks passed:
  - `pytest tests/test_auth_service.py tests/test_api.py -k 'login or issued_api_key'` (6 tests)
- **Learnings for future iterations:**
  - Story splits around login/auth can overlap; when that happens, validate the existing acceptance contract first and only add code if the implementation actually falls short
  - The backend contract for issued keys is already anchored in `POST /login` plus `AuthService`; future auth stories should extend key lifecycle behavior there instead of creating a parallel issuance path
---

## 2026-04-18 13:05 -03 - US-035
- Persisted the issued frontend session in `sessionStorage` through `frontend/src/lib/api.ts` so the workspace can restore the active `X-API-Key` without manual operator input
- Added automatic session invalidation handling for authenticated API responses and wired the app shell to return to the login screen when the key becomes invalid
- Added an explicit logout action in the frontend shell that clears the active session state and stored key
- Replaced protected artifact/export download links with authenticated download helpers so report/result/CSV downloads also send `X-API-Key`
- Added focused frontend coverage for session persistence, auth-header injection, logout, invalid-session redirect, and protected downloads
- Files changed: `frontend/src/lib/api.ts`, `frontend/src/components/result-workspace.tsx`, `frontend/src/app/page.tsx`, `frontend/src/app/globals.css`, `frontend/src/lib/__tests__/api.test.ts`, `frontend/src/app/page.test.tsx`, `AGENTS.md`, `prd.json`, `progress.txt`
- Checks passed:
  - `cd frontend && npm run typecheck`
  - `cd frontend && npm run test` (44 tests)
  - `cd frontend && npm run lint`
  - `ruff check app tests`
  - `pytest` (357 tests)
- Browser verification: no browser-testing connector was available in this session; manual UI verification is still recommended for the login/logout/download flow.
- **Learnings for future iterations:**
  - Keep frontend auth state centralized in `frontend/src/lib/api.ts`, including invalid-session callbacks, so page-level routing can react without duplicating 401 handling across components
  - Protected downloads must use authenticated fetch helpers rather than raw anchors because the browser will not attach `X-API-Key` automatically to `<a href>` requests
  - `sessionStorage` fits the current operator flow better than in-memory state alone because it survives refresh without turning the issued key into a longer-lived local artifact
---

## 2026-04-18 13:17 -03 - US-036
- Implemented issued-key lifecycle management with tenant-configured TTL in `tenant.auth.issued_api_key_ttl_seconds`, storing `issued_ttl_seconds`, `expires_at`, `revoked_at`, and `expired_at` on issued key records
- Added audit emission for `api_key_issued`, `api_key_expired`, and `api_key_revoked`, keeping the lifecycle logic centralized in `app/services/auth_service.py`
- Updated auth middleware to reject expired or revoked issued keys with consistent `401` responses before falling back to legacy tenant `api_keys`
- Exposed `POST /api-keys/revoke` for tenant-scoped revocation of the current or explicitly selected issued key without touching historical job/audit artifacts
- Added focused coverage for tenant auth policy loading, issued-key expiration metadata, single-shot expiration audit logging, revocation, API audit visibility, and 401 handling for expired/revoked keys
- Files changed: `app/core/tenant_config.py`, `app/core/audit.py`, `app/services/auth_service.py`, `app/api/auth.py`, `app/api/routes.py`, `app/tenants/default/tenant.yaml`, `tests/test_tenant.py`, `tests/test_auth_service.py`, `tests/test_api.py`, `AGENTS.md`, `prd.json`, `progress.txt`
- Checks passed:
  - `pytest tests/test_auth_service.py tests/test_tenant.py tests/test_api.py -q`
  - `ruff check app tests`
  - `pytest -q`
- **Learnings for future iterations:**
  - Keep the login-issued key lifecycle in `AuthService`; routes should only expose login/revocation endpoints and middleware should consume the resolved status instead of duplicating expiry logic
  - Tenant-owned auth policy belongs in typed config (`tenant.auth`) so TTL changes stay declarative and do not require route-level branching
  - Expiration audit should be emitted only once on the first failed use of a stale key; persisting `expired_at` avoids duplicate audit noise on repeated requests
---

## 2026-04-18 14:25 -03 - US-037
- Implementado `AuthService.renew_api_key` que emite novo `X-API-Key` e revoga o registro anterior na mesma transação, preservando tenant/operador e respeitando o TTL tenant-configurável
- Adicionado endpoint autenticado `POST /api-keys/renew` que encapsula o fluxo de renovação usando o contexto resolvido pelo middleware, retornando `previous_api_key_id`, nova `x_api_key` e `expires_at`
- Adicionado evento `AuditEventType.API_KEY_RENEWED` com detalhes ligando predecessor e sucessor (`successor_api_key_id`, `renewed_at`, `expires_at`)
- `LoginResponse` e persistência de sessão no frontend passam a propagar `expires_at`; `renewApiSession` atualiza o storage com a chave renovada e mantém o handler de invalidação existente para falhas 401
- Página inicial do frontend mostra banner com botão `Renovar sessão` quando faltam ≤ 5 min para expirar e trata erros de renovação sem forçar logout
- Files changed: `app/core/audit.py`, `app/services/auth_service.py`, `app/api/routes.py`, `tests/test_auth_service.py`, `tests/test_api.py`, `frontend/src/lib/types.ts`, `frontend/src/lib/api.ts`, `frontend/src/app/page.tsx`, `frontend/src/app/globals.css`, `frontend/src/lib/__tests__/api.test.ts`, `frontend/src/app/page.test.tsx`, `AGENTS.md`, `prd.json`, `progress.txt`
- Checks passed:
  - `ruff check app tests`
  - `pytest` (371 tests)
  - `cd frontend && npm run typecheck`
  - `cd frontend && npm run test` (49 tests)
  - `cd frontend && npm run lint`
- Browser verification: no browser-testing connector was available in this session; manual UI verification is still recommended for the renewal banner and timer.
- **Learnings for future iterations:**
  - Renewal must revoke and issue in the same `AuthService` call so the predecessor key becomes invalid atomically with the successor creation, keeping middleware and audit aligned
  - The `API_KEY_RENEWED` event should carry `successor_api_key_id` so downstream tooling can follow a session lineage without inspecting raw key hashes
  - Frontend session clock watching should rely on `Date.now` + a light `setInterval`; use `vi.spyOn(Date, 'now')` in tests instead of `vi.useFakeTimers`, which stalls React Testing Library's internal timers
---
